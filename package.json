{
  "name": "bbop-js-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for BBOP JS packages",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "lint": "oxlint .",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "release:version": "lerna version",
    "release:publish": "lerna publish",
    "test": "lerna run test"
  },
  "devDependencies": {
    "@tsconfig/node-lts": "^24.0.0",
    "lerna": "^9.0.7",
    "oxfmt": "^0.47.0",
    "oxlint": "^1.62.0",
    "tsdown": "^0.21.10",
    "typescript": "^6.0.3"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
